# Update Subtitle Transition to "Software Engineering Manager <> PM"

**Date:** 2026-08-26  
**Tags:** #ui #copywriting #hero #subtitle #engineering-manager #product-manager

## Context
Updated the hero transition subtitle from `"Software Engineering Manager → PM"` (directional arrow) to **`"Software Engineering Manager <> PM"`** (bidirectional bridge) in `page.tsx` and `remoteConfig.ts`.

## Reflections
- The `<>` notation emphasizes dual capability and bidirectional synthesis across engineering management and product leadership rather than a one-way historical transition.

## Decisions
1. **Hero Subtitle:** Updated line in `page.tsx` to render `Software Engineering Manager <> PM`.
2. **Remote Config Preset:** Updated `VARIANT_PRESETS.action_oriented.hero_subtitle_variant` in `remoteConfig.ts`.
